export enum SourceSyncProgressStage {
  FETCHING = "FETCHING",
  VERIFIED = "VERIFIED",
  VERIFYING = "VERIFYING",
}

export enum SourceSyncResultType {
  SOURCE_SYNCHRONIZATION = "source-synchronization",
}

export enum SourceSyncSummarySchemaVersion {
  V1 = "sylis.source-synchronization/1",
}
